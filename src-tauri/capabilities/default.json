{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs plus OS notifications and clipboard read for the main window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default", "clipboard-manager:allow-read-text"]
}
