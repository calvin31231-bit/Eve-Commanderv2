{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs plus OS notifications for the main window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
